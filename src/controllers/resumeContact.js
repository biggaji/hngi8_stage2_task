exports.processForm = async (req,res) => {
    const { fullname, email, phone, msg } = req.body;

    
    // get save data to database ,
    // then message your mail for new message alert
    // also a mail to the sender so i can process it

};